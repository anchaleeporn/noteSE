git init คือใช้สำหรับจองโฟล์เดอร์สำหรับเก็บข้อมูลใช้ได้ครั้งเดียว

git init คือ โฟล์เดอร์ไว้เก็บของ ได้ไฟล์ .git ใช้ครั้งเดียวต่อโฟร์เดอร์ : JAJA

git init คือการสร้างไฟล์เป็นไฟล์ .git 

git init คือ การสร้างพื้นที่ว่างไว้เก็บการใช้งาน

git init คือ การสร้างพื้นที่ว่างไว้เก็บงาน